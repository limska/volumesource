#include "Translate.h"

void
Translate::
calculate(Mesh & mesh, Vector const & vector) const
{
  // for (Vertex & vertex : mesh.verts)
  // {
  //   vertex[0] += vector[0];
  //   vertex[1] += vector[1];
  //   vertex[2] += vector[2];
  // }
}